

//########## Teachers ########//

//will give you the answer but in a useless format
char* yotka(char* argv[]){
  //psudocode for now
  return (*char)(eval(arg));
}

//will calculate your result and show it for you but not give you anything to work with
void nasim(char* argv[]){
  //psudocode for now
  printf((*char)(eval(arg)));
}

//god is there for you and will help with anything you need
double* nasim(char* argv[]){
  //psudocode for now
  return (eval(arg));
}

//wrapper for printf basically
void jen(char* argv[]){
  //psudocode for now
  printf(arg));
}

//########### classes ##########//



//############ memes ############//
/** 
    figure out how to 
    catch SEGMENTATION_FAULT
    then -> puts("A wild tiger corrupted your memory")
    exit(0)    // -> remove informative exception for now
    catch GENERAL_EXCEPTION
    then -> puts("BAM tiger")
    exit(0)    // -> remove informative exception for now
**/
